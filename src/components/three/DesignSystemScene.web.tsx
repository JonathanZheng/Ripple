// Web fallback — uses the same animated orb background as native.
// Three.js (@react-three/fiber) is ESM-only and cannot be bundled by Metro for web.
export { DesignSystemScene } from './DesignSystemScene.native';
